# Codex launch file. Read this only, run the OS command, stop reading unless launch fails.
# Do not scan source, README, docs, node_modules, .venv, dist, logs, cache, or token files.
#
# Windows PowerShell:
#   Set-ExecutionPolicy -Scope Process Bypass
#   .\scripts\run_windows.ps1
#
# macOS/Linux shell:
#   chmod +x scripts/setup_mac.sh scripts/run_mac.sh
#   ./scripts/run_mac.sh
#
# The scripts attempt to install missing system/app dependencies, launch backend/frontend, and open:
# http://127.0.0.1:5173
#
# If blocked by OS permissions, follow the script's last printed instruction, reopen terminal, rerun.
# Share folder without:
# frontend/node_modules, frontend/dist, backend/.venv, backend/.cache,
# backend/.kite_uat_session.json, logs, pid files.

-r backend/requirements.txt
